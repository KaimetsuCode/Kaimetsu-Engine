#include "Camera.hpp"

float DefaultCamera::yaw = -90.0f;
float DefaultCamera::pitch = 0.0f;
float DefaultCamera::speed = 0.01f;
float DefaultCamera::sensitivity = 0.25f;
float DefaultCamera::zoom = 45.0f;